# Dark Workbench DESIGN

更新时间：2026-06-17

## 采用策略

`awesome-design-md` 在本项目中作为设计规范结构参考，而不是视觉风格来源。

- 采用：DESIGN 文档结构、token 分层、组件状态表、页面模板、禁止规则、验收清单。
- 不采用：营销页构图、品牌级插画、过度装饰、网站式大留白、单一色相渐变主题。
- 参考方向：高密度 Windows 桌面工具、Data-Dense Dashboard、Admin Tool。
- 验证方式：以 1180x720 和 1440x860 的 smoke 截图、JSON smoke、标准脚本输出为准。

## 产品界面目标

Dark Workbench 是虾米工具箱的新壳层，首要目标不是“更炫”，而是让长期操作更稳定：

- 页面入口完整，旧主界面已有真实功能页不能在新版壳层遗漏。
- 高频页面在 1180x720 下尽量减少横向截断和无意义滚动。
- 表格、路径、参数矩阵、日志和生成结果优先可读。
- 主操作明确，次操作可见但不抢视觉层级。
- 页面之间的边距、按钮高度、表单行高、面板圆角和状态色保持一致。

## Token 分层

| 层级 | 代码落点 | 说明 | 禁止 |
| --- | --- | --- | --- |
| ThemeTokens | `dark_workbench_shell.py` 的主题/QSS 生成 | 颜色、背景、边框、状态色 | 页面内随手写裸色值 |
| UiMetrics | `工具箱_qt.py` 的 `UiMetrics` 或同级常量 | 控件高度、最小宽、行距、面板间距 | 同类控件散落 magic number |
| ComponentRoles | `setProperty()`、`objectName`、统一 helper | 按钮角色、字段状态、面板角色 | 每个按钮单独 `setStyleSheet` |
| LayoutTemplates | 页面 builder/helper | Splitter、表格工具页、生成器页、双栏页 | 复杂页面临时堆控件 |
| ValidationRules | `scripts\test.ps1`、smoke 参数、截图 | 每批结果证据 | 只凭肉眼判断完成 |

## 核心组件状态

### Button

| 状态 | 适用场景 | 视觉要求 | 验收点 |
| --- | --- | --- | --- |
| primary | 每页 0-1 个主提交动作 | 蓝色实底，宽度足够承载最长文本 | 1180x720 不截字 |
| secondary | 刷新、预览、打开、说明 | 深色描边或弱底色 | 不抢 primary |
| danger | 删除、清空不可逆动作 | 红色边框或弱红底 | 与普通按钮明显区分 |
| compact | 表格行内、工具条按钮 | 固定高度，小间距 | 不改变行高 |

### Field

| 状态 | 适用场景 | 视觉要求 | 验收点 |
| --- | --- | --- | --- |
| normal | 常规输入 | 与全局控件高度一致 | label 和输入框基线稳定 |
| path | 文件夹/文件路径 | 可横向扩展，按钮不被挤掉 | 长路径下按钮仍可见 |
| number | 数量、坐标、天数 | 宽度受控 | 不占用整行无效空间 |
| readonly | 输出或派生值 | 弱背景、可复制 | 不误判为可编辑 |
| error/warning/valid | 校验状态 | 使用统一状态色 | 不只靠文字提示 |

### Panel

| 类型 | 适用页面 | 视觉要求 | 验收点 |
| --- | --- | --- | --- |
| workspace | 双栏主工作区 | Splitter 两侧有最小宽度 | 分割线不吃掉按钮 |
| table-tool | 表格 + 工具栏 | 表格区域优先高度 | 底部按钮不挤压表格 |
| generator | 参数矩阵 + 生成动作 | 参数行紧凑，主按钮清楚 | 1180x720 关键行可见 |
| help | 说明/脚本示例 | 可折叠或低占高 | 不压缩主任务区 |

## 页面模板

## 全局工程上下文

服务端根目录属于全局工程上下文，不属于单个功能页。

- 顶部工程栏负责展示当前工程根目录、版本/索引状态和切换入口。
- 功能页默认不得重复显示“选择根目录”大卡片；需要根目录时从全局工程上下文读取。
- 页面只保留与当前任务直接相关的路径，例如数据库文件、输出目录、脚本文件或临时覆盖路径。
- 少数需要处理非当前工程目录的功能，使用“高级/覆盖路径”折叠区，默认收起。
- 小批次迁移顺序：先保留隐藏控件和旧同步逻辑，移除页面重复路径行；验证 smoke 后再抽象统一根目录 API。

### SplitWorkspacePage

用于 `存销设置`、`回收生成`、`脚本注入` 等左右工作区页面。

- 左侧承载列表、表格或输入矩阵。
- 右侧承载预览、脚本、说明或批量操作。
- 必须设置 splitter 策略和两侧最小宽度。
- 页面变窄时优先保留主任务区，次要说明区允许滚动。

### TableToolPage

用于 `会员系统`、`CDK 管理`、`文本搜索` 等表格页。

- 表格高度优先于说明区。
- 顶部路径/筛选行不得把按钮挤出可视区。
- 行内长文本允许省略，但关键列标题不能被压到不可读。

### GeneratorPage

用于 `货币兑换`、`刷怪设置`、`物品注入` 等生成类页面。

- 参数矩阵行距应比普通表单更紧凑。
- 主生成/写入按钮只能有一个最高视觉层级。
- 1180x720 下应至少露出完整参数区和主要操作区；如仍需滚动，滚动必须是有意义的内容延展。

## 当前项目禁用规则

- 不新增营销型 hero、装饰性插画、渐变背景球或大幅空白首屏。
- 不用页面局部 QSS 覆盖同类全局控件，除非该页已有专用对象名并能说明理由。
- 不为了低高度屏幕缩小全局字号。
- 不在一个批次同时重做多个无关页面。
- 不对 `网站管理` 做额外 UI 打磨，除非只是启动、挂载或 smoke 维护。
- 不移除已恢复入口：`刷怪设置`、`存销设置`、`回收生成`、`变量查询`、`封挂编码/编码转换`、`免费微端`、`脚本注入`。

## 小批次验收清单

每个 Dark Workbench UI 批次至少记录：

- 目标：一个页面或一类共性问题。
- 入口矩阵：确认 `artifacts\dark-workbench-smoke.json` 仍包含 20 页和已恢复入口。
- 改动文件：优先 `工具箱_qt.py`、`dark_workbench_shell.py` 或 `scripts\`。
- 验证命令：代码改动至少 `.\scripts\build.ps1 -ValidateOnly`，UI 改动运行 `.\scripts\test.ps1`。
- 截图产物：保存并查看对应 `artifacts\smoke-*.png`。
- 剩余风险：说明仍需要滚动、业务未深测或只做布局验证的部分。
- 下一步建议：只给一个最小候选批次。

## 接入方式

后续 UI 批次按以下顺序使用本文档：

1. 先看 `AGENTS.md` 的当前追求目标和禁改范围。
2. 再用本文档判断页面属于哪类模板。
3. 修改前确认该页当前 smoke 截图的问题证据。
4. 修改时优先调整 token、metrics、role 或模板策略。
5. 修改后用 smoke JSON 和截图证明没有入口回归。

## 2026-06-18 全页面自查整改基线

本节基于当前 `artifacts/smoke-*.png` 截图自查，并合并外部逐图评审意见。后续 Dark Workbench UI 批次必须先满足本节约束，再做局部视觉美化。

### 结论

当前主要问题不是单个按钮、配色或圆角，而是信息架构失衡：

- 导航入口过多且互相重复，用户无法判断真实功能归属。
- 顶部通栏把工程信息、运营状态和窗口控件混排，所有页面都继承同一视觉混乱。
- 底部 Dock、打开按钮和日志常驻混排，占用主体高度并干扰操作。
- 页面内部缺少统一模板，控制区、说明区、结果区和主表格经常平铺堆叠。
- 主操作、常规操作、次要操作和危险操作没有稳定视觉规则。
- 长路径、链接、密码、变量、CDK、脚本等关键产物没有高亮和快捷复制能力。

工程 smoke 只能证明页面可启动、入口未回归、控件未明显溢出；不能证明布局合理。每次 UI 批次必须同时进行截图审查。

### P0 信息架构规则

#### 导航归属

- 侧边栏是唯一完整功能目录，每个真实功能只能有一个主归属。
- HO 首页保留，但降级为工程状态驾驶舱，不再作为全功能按钮聚合页。
- 底部 Dock 只保留 5 个以内最高频任务切换，不复制完整侧边菜单。
- 左侧顶部 `重载 / 函数库 / 管理器 / 更新` 必须归入明确的全局工具区，不能悬浮在功能菜单之外。
- 重复入口先改成别名跳转或弱化快捷，不直接删除真实功能，避免旧入口回归争议。
- `货币兑换`、`文件对比` 等重复存在于侧边独立页面和 `BS` 子标签的功能，必须建立唯一真实页面和入口映射表。
- 侧边缩写必须带中文后缀或 tooltip，例如 `SP 刷怪设置`、`VQ 变量查询`、`SI 脚本注入`。
- 当前页选中态必须同时体现背景、图标色、文字色或字重变化，不能只靠浅色底。

#### 顶部通栏

- 顶部禁止在同一行混放工程路径、业务状态、运营指标和窗口按钮。
- 目标结构为两层：第一层为窗口标题、工程路径、版本信息；第二层为业务状态卡片和窗口控件。
- 窗口最小化、最大化、关闭按钮必须与业务操作视觉隔离。
- 当前页面路径和页面标题必须比状态卡片更容易识别，不能被顶部运营指标抢权重。

#### 底部区域

- 底部日志默认不常驻占用主界面高度，应改为可折叠面板或开发态面板。
- 日志区必须提供清空、暂停刷新和展开/收起能力。
- Dock 按钮必须有可识别文字或 tooltip，纯缩写不能作为唯一信息。
- 底部主操作按钮不能大到破坏 Dock 比例；打开/截图等操作应属于当前页动作或 Dock 工具组。

### P1 页面模板规则

#### 内容区优先

- 主体高度优先给表格、列表、编辑器、预览、结果文本等真实内容。
- 路径、状态、说明、连接工具只能作为紧凑控制带，不得形成大面积空白面板。
- 页面说明默认弱化或折叠，开发验收信息不得进入生产首页。
- 操作按钮必须围绕被操作对象就近组织：表格操作在表格工具条，结果操作在结果区，连接操作在连接带。

#### 模块分区

- 所有页面必须明确区分配置区、操作区、结果区、状态区。
- 卡片用于真实功能模块，不用于把整个页面逐层套卡片。
- 同一页面卡片内边距、标题、工具条高度必须一致。
- 空白必须服务于分组或可读性，不能出现无信息、无操作的大块空洞。

#### 多栏比例

- 双栏页面必须设置最小可用宽度，避免左栏列表窄到不可读或右栏表单挤压。
- 表格主页面优先采用 `主表格 + 右侧详情/工具`，主表格不能低于可视宽度的 60%。
- 参数生成页面可采用 `左配置 + 右预览/结果`，预览区不能挤占主配置路径和主按钮。
- 三栏页面必须有明确对象关系，例如 `列表 -> 详情 -> 参数`，不得只是把控件平均铺开。

### P1 按钮和文字规则

#### 按钮分级

- `primary`：每页 0-1 个当前主任务，如生成、保存、一键注入、开始扫描。
- `secondary`：浏览、刷新、备份、读取配置等普通操作，不能抢 primary。
- `tertiary`：复制、清空、上一处、下一处等低风险辅助动作，用轻量描边或文本按钮。
- `danger`：删除、清空全部、一键删除、回滚、批量重置等风险动作，必须使用橙红视觉并触发二次确认。
- 标签页、分页、筛选 chip 不能复用功能按钮视觉。

#### 文本可读性

- 路径、链接、密码、端口、变量 ID、CDK、地图编号、脚本代码属于关键产物，必须高亮或提供结构化展示。
- 长路径和长文本必须支持换行、悬浮完整预览或复制按钮，不能只靠横向拖拽。
- 报错、警告、过期、缺失配置必须使用统一橙红状态色，不能只用浅灰小字。
- 表格长字段必须有 tooltip 或详情区，不能让截断成为唯一阅读方式。

### P2/P3 交互规则

- 路径、链接、密码、变量、QQ 群、CDK、地图编号、脚本片段都应逐步补齐单行复制。
- 表格类页面统一提供顶部搜索/筛选能力，并显示记录数和当前筛选摘要。
- 扫描、生成、批量修改、注入、编码转换等耗时操作完成后必须有明确成功/失败反馈。
- 风险操作必须有二次确认，确认文案说明影响范围。
- 页面内跳转不能伪装成普通文字，应使用明确的按钮、链接样式或菜单项。

### 当前截图自查补充

- `smoke-overview.png`：HO 首页功能入口过多，开发态验收信息占据生产首屏，应改为状态驾驶舱。
- `smoke-db.png`：连接区右侧空白大，表格实际信息区过小，表格操作应并入紧凑工具条。
- `smoke-free-micro.png`：配额、启动、PID、采集结果缺少模块边界；链接和密码是核心产物，不应作为普通日志文本。
- `smoke-script.png`：说明、路径、模板列表、执行按钮和表格没有清晰操作顺序；一键注入属于高风险主操作，需要确认和视觉强调。
- `smoke-website.png`：多标签、根目录、进度、远端控制堆叠拥挤。该页后续功能会大改，当前只作为问题样本，不优先做深度 UI 打磨。
- `smoke-spawn.png`：左表格和右批量工具比例不稳，右侧工具面板窄且滚动，主保存按钮离表格操作对象过远。
- `smoke-var-query.png`：变量分类按钮密集且语义弱，详情区和已占用列表比例不清，扫描完成反馈弱。

### 实施顺序

1. P0：建立入口归属表，收敛四层导航，处理重复入口为唯一真实页面加别名跳转。
2. P0：重构顶部通栏和底部日志/Dock 职责边界。
3. P1：统一四类按钮视觉和危险操作确认规则。
4. P1：落地页面模板，优先修正 `数据库管理`、`免费微端`、`脚本注入`、`刷怪设置`、`变量查询`。
5. P2/P3：补齐复制、筛选、tooltip、完成反馈和页面专属细节。

每批仍按小批次执行：只处理一个框架问题、一个页面模板或一个页面；改动后运行 `.\scripts\build.ps1 -ValidateOnly`，UI 改动再运行 `.\scripts\test.ps1` 并检查 smoke 截图。

## 入口归属表与导航整改方案

本节用于约束 P0 导航重构。目标不是减少功能，而是让每个功能有唯一真实归属，其他位置只承担快捷或别名跳转。

### 导航层职责

| 导航层 | 保留职责 | 必须移除或弱化的职责 |
| --- | --- | --- |
| 左侧侧边栏 | 完整功能目录、唯一真实页面入口、模块分组 | 纯字母无释义、入口重复但不说明归属 |
| HO 首页 | 工程状态驾驶舱、最近任务、异常提醒、3-5 个继续工作动作 | 全功能按钮聚合、开发验收文本、重复完整菜单 |
| 底部 Dock | 5 个以内高频任务切换、当前工作区快捷打开 | 复制侧边完整菜单、长期占用大面积日志 |
| 顶部全局工具 | 工程路径、版本、全局状态、窗口控制 | 页面功能入口、全局工具悬浮在侧边菜单外 |
| 页面内标签 | 同一真实页面内部的视图切换 | 与侧边独立页面重复实现同一功能 |

### 唯一真实页面归属

| Key | 中文功能 | 主分组 | 唯一真实页面 | 允许别名/快捷 | Dock 默认 | 整改动作 |
| --- | --- | --- | --- | --- | --- | --- |
| overview | 主页概览 | 状态 | HO 工程状态驾驶舱 | 启动默认页 | 否 | 移除全功能按钮墙，只保留状态、异常、最近任务 |
| micro | 微端配置 | 基础工具 | 侧边 `MC 微端配置` | HO 最近任务可跳转 | 可选 | 保留真实页面，Dock 仅在高频配置阶段出现 |
| free_micro | 免费微端 | 基础工具 | 侧边 `FM 免费微端` | HO 最近任务可跳转 | 是 | 保留 Dock，但文字/tooltip 必须清晰 |
| real_basic | 基础设置 | 基础工具 | 侧边 `BS 基础设置` | 无 | 否 | 仅保留基础设置和端口设置等本页内部配置 |
| map_settings | 地图设置 | 基础工具 | 侧边 `MP 地图设置` | 无 | 否 | 保留真实页面 |
| search | 文本搜索 | 基础工具 | 侧边 `SE 文本搜索` | BS 中可提供跳转别名 | 否 | 避免在 BS 内重复维护完整搜索页 |
| compare | 文件对比 | 基础工具 | 侧边 `CP 文件对比` | BS 子标签改为跳转/别名 | 否 | 建立唯一真实页面，保留老入口跳转 |
| db | 数据库管理 | 版本数据 | 侧边 `DB 数据库管理` | HO 最近任务可跳转 | 是 | 保留 Dock，优先作为数据类高频页 |
| currency | 货币兑换 | 版本数据 | 侧边 `CY 货币兑换` | BS 子标签改为跳转/别名 | 否 | 建立唯一真实页面，避免双实现 |
| drop | 爆率管理 | 版本数据 | 侧边 `DR 爆率管理` | 无 | 否 | 保留真实页面 |
| spawn | 刷怪设置 | 版本数据 | 侧边 `SP 刷怪设置` | HO 最近任务可跳转 | 是 | 保留 Dock，属于高频版本数据页 |
| store | 存销设置 | 版本数据 | 侧边 `ST 存销设置` | 无 | 否 | 保留真实页面 |
| recycle | 回收生成 | 版本数据 | 侧边 `RC 回收生成` | 无 | 否 | 保留真实页面 |
| var_query | 变量查询 | 版本数据 | 侧边 `VQ 变量查询` | HO 最近任务可跳转 | 是 | 保留 Dock，属于排查类高频页 |
| encoding | 封挂编码 | 版本数据 | 侧边 `EN 封挂编码` | 无 | 否 | 保留真实页面 |
| script | 脚本注入 | 版本数据 | 侧边 `SI 脚本注入` | HO 最近任务可跳转 | 是 | 保留 Dock，但一键注入必须按危险主操作处理 |
| item | 物品注入 | 版本数据 | 侧边 `IT 物品注入` | 无 | 否 | 保留真实页面 |
| cdk | CDK 管理 | 运营工具 | 侧边 `CK CDK 管理` | 无 | 否 | 保留真实页面 |
| member | 会员系统 | 运营工具 | 侧边 `MB 会员系统` | 无 | 否 | 保留真实页面 |
| website | 网站管理 | 运营工具 | 侧边 `WB 网站管理` | 无 | 否 | 后续功能大改前只做必要挂载和 smoke 维护 |

### 重复入口处理规则

- 不直接删除历史入口；先改成跳转、别名或说明入口，确认用户路径稳定后再考虑移除。
- `BS 基础设置` 内的 `货币兑换`、`文件对比` 子标签后续不得继续作为两套真实页面维护。
- 入口跳转必须明确显示目标真实页面名称，例如 `已移动到：CY 货币兑换`。
- smoke 必须覆盖真实页面和别名跳转能力，避免入口回归。
- 任何新增页面必须先登记到本表，再进入侧边栏、Dock 或 HO 快捷区。

### Dock 收敛规则

- 默认 Dock 保留：`free_micro`、`db`、`spawn`、`var_query`、`script`。
- Dock 按钮必须显示中文短名或 tooltip，不允许只显示 `FM/DB/SP/VQ/SI`。
- Dock 只用于快速切换，不承载页面主操作；页面主操作必须回到页面内容区。
- `截图`、`打开` 属于工具动作，后续应进入 Dock 工具组或开发态工具，不应与功能入口混为一组。
- 日志不属于 Dock 固定内容，必须改为可折叠或开发态面板。

### P0 导航批次建议

1. 第一批：侧边栏中文后缀、tooltip、选中态强化；不改变真实页面数量。
2. 第二批：HO 首页移除全功能按钮墙，改为状态驾驶舱和最近任务。
3. 第三批：底部 Dock 收敛为 5 个高频入口，日志改为可折叠面板。
4. 第四批：`BS` 重复子标签改为真实页面跳转别名，并补 smoke。
