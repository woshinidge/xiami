# miniz

Vendored from [richgel999/miniz](https://github.com/richgel999/miniz), tag
`3.1.0`. Only the inflate implementation is compiled by Xiami.

Upstream files are kept unmodified. `miniz_export.h` is the local static-link
equivalent of the header normally generated by the upstream CMake build.

License: MIT (`LICENSE`).

