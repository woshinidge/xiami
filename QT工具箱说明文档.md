# 说明文档

## 1. 项目规划
本项目旨在将现有的 C# "NPC拖拽框架" 完全转换为 Python 语言版本。
目标是保留原有的拖拽、界面交互及核心逻辑，并适配 Python 的 GUI 框架（优先使用 Tkinter 以保持与现有工具箱技术栈一致）。

## 2. 实施方案
### 2.1 分析阶段
- [x] 阅读现有 C# 代码，理解业务逻辑、数据结构及 UI 布局。
- [x] 确定 Python 技术选型（Tkinter）。

### 2.2 开发阶段
- [x] 搭建 Python 项目结构。
- [x] 逐步迁移各个组件：
    - [x] 工具类 (Coordinator) - 实现完整代码生成逻辑
    - [x] 辅助控件 (PropertyPanel, CodePanel) - 支持属性白名单与颜色选择器
    - [x] 窗体 (MainWindow) - 实现三栏布局与工具栏
    - [x] 核心控件 (DraggableControl) - 支持所有 Mir 控件属性与视觉差异
- [x] 实现核心拖拽功能。
- [x] 实现属性双向绑定。
- [x] 实现高级代码生成（支持位置计算与绝对坐标模式）。
- [x] 实现网格背景绘制。
- [x] 实现代码反向解析（支持从脚本生成控件）。
- [x] 实现代码编辑器语法高亮。

### 2.3 测试阶段
- [x] 验证界面布局是否还原（通过 headless 测试验证逻辑）。
- [x] 测试拖拽功能是否流畅。
- [x] 确保所有功能点与原版一致。
- [x] 验证代码生成格式的准确性（已通过单元测试）。
- [x] 验证代码反向解析逻辑（已通过单元测试）。

## 3. 进度记录
| 时间 | 任务 | 状态 | 说明 |
| :--- | :--- | :--- | :--- |
| 2026-01-21 | 项目启动 | 完成 | 创建文档，分析C#代码 |
| 2026-01-21 | Python框架搭建 | 完成 | 创建项目目录结构 |
| 2026-01-21 | 核心功能实现 | 完成 | 实现拖拽、属性编辑、基础代码生成 |
| 2026-01-21 | 功能完善 | 完成 | 实现完整代码生成逻辑、网格绘制、颜色选择器、控件视觉优化 |
| 2026-01-21 | 验证与测试 | 完成 | 编写并运行自动化测试脚本，验证核心逻辑 |
| 2026-01-21 | 功能增强 | 完成 | 实现代码到UI的解析同步、编辑器语法高亮、控件视觉优化 |
| 2026-01-21 | 辅助功能开发 | 完成 | 实现资源选择对话框（Wil/图片/物品）、Wil资源加载基础架构 |
| 2026-01-21 | 交互优化 | 完成 | 增加右键菜单（删除/层级调整）、Delete快捷键、本地图片加载Mock |
| 2026-01-22 | 文本解析与排版补齐 | 完成 | 支持行内文本解析、自动排版与输入控件行内解析，并通过测试 |
| 2026-01-22 | 补齐默认属性与布局约束 | 完成 | 补齐控件默认属性、容器内可见性约束与角括号匹配解析，并通过测试 |
| 2026-01-22 | 修复资源选择器参数错误 | 完成 | 修复属性面板调用资源选择器的参数不匹配问题，并通过测试 |
| 2026-01-30 | 更新存销QFunction注入脚本 | 完成 | 替换[@掉落前检测]下注入逻辑并通过pytest验证 |
| 2026-01-30 | 支持外部咕咕鸡过滤脚本覆盖 | 完成 | 生成存销时优先使用根目录咕咕鸡过滤.txt并通过pytest验证 |
| 2026-02-10 | QT工具箱打包参数记录 | 完成 | 固定生成dist更新包，zip内无_internal，支持直接运行 |
| 2026-03-23 | 网站查询改为全量数据虚拟滚动+自动续载显示 | 完成 | 已移除前端截断与底部翻页/状态提示，普通列表与地图走法均改为滚动到底自动续载；物品、怪物、地图、NPC及相关明细均支持全量浏览 |
| 2026-03-23 | 网站模板全面审查与UI兼容优化 | 完成 | 已审查默认网站模板与生成页样式，优化响应式布局、视觉一致性、交互可用性与首页轮播性能；保持 `site.json`、`drop_queries`、版本切换与数据加载接口兼容 |
| 2026-03-23 | 首页视觉层级重构与多分辨率适配增强 | 完成 | 已重构首页 Hero、信息卡片与空状态展示，补充内容模块并优化按钮/面板在不同分辨率下的布局表现；同步保持现有后台接口与链接行为不变 |
| 2026-03-23 | 下载中心独立页面重构 | 完成 | 已将 `download.html` 从跳转页改为独立下载中心，新增版本下载区、扩展资源区与帮助信息区；保持站点配置字段、下载链接与爆率查询入口兼容 |
| 2026-03-23 | 修复网站生成公告状态变量未定义 | 完成 | 已补回首页模板中的 `announcement_ready` 状态变量定义，解决“生成/更新”时报错 `name 'announcement_ready' is not defined` 的问题，并恢复站点正常生成 |
| 2026-03-23 | 删除首页内容编排展示区块 | 完成 | 已移除首页中“更具层次感的内容编排”整段展示区块，保留 Hero、版本区与公告/社群信息区，页面结构更简洁 |
| 2026-03-23 | 传奇官网风格重构与交互链路优化 | 完成 | 已将首页、下载中心、爆率查询页统一为传奇官网风格的暗红金视觉；补齐首页到下载中心/爆率页导航、爆率页内联站点数据启动、`drop_enabled/drop_reverse/show_rate` 关键控制逻辑，提升后台配置可用性、首屏速度与交互稳定性 |
| 2026-03-23 | 精简首页提示文案并移除下载页 | 完成 | 已删除首页 Hero/版本区/信息区的额外提示文案与状态标签，移除站点中的下载按钮、下载页入口及 `download.html` 输出，同时保留版本展示、宣传视频、公告社群与爆率查询功能可用 |
| 2026-03-23 | 移除内置网站模板右侧介绍卡片 | 完成 | 已删除首页 Hero 区右侧“门户定位/视频状态/版本入口”等介绍卡片；无视频时自动切换为单栏布局，有视频时仅保留视频展示，避免默认模板出现占位介绍内容 |
| 2026-03-23 | 调整爆率首页版本按钮与结果区滑动模块 | 完成 | 已将爆率首页版本卡片中的“确定”改为按后台版本下载地址显示的“版本下载”按钮，未配置下载地址时自动隐藏；同时将查询结果四列改为横向滚动滑动模块展示，保留列内滚动、虚拟列表与自动续载能力 |
| 2026-03-23 | 爆率结果区改为四列独立纵向滚动 | 完成 | 已取消结果区左右滑动布局，改为桌面端四列/中屏两列/移动端单列展示；每列列表均支持独立平滑纵向滚动，兼容桌面滚轮、触控板与移动端触摸滑动 |
| 2026-03-23 | 爆率结果区列头固定优化 | 完成 | 已为四列查询模块增加固定列头效果，标题与过滤框在列内保持可见，下方列表继续独立滚动；同步优化暗色毛玻璃背景与层级阴影，提升桌面端和移动端浏览稳定性 |
| 2026-03-23 | 爆率结果区内容改为列内滑动展示 | 完成 | 已为四个垂直列内容区补充列内滑动展示效果：列表容器增加平滑纵向滑动、滚动捕捉与上下渐隐遮罩，地图走法卡片同步改为滑动卡片流；避免内容直接铺满页面，兼容桌面端滚轮/触控板与移动端触摸滑动 |
| 2026-03-23 | 爆率结果区固定高度独立滚动模块强化 | 完成 | 已将四列查询区统一为固定高度滚动模块：桌面端使用视口自适应模块高度、移动端使用独立紧凑高度，每列头部固定、内容区独立滚动，避免页面被长列表整体拉长并提升多端浏览体验 |
| 2026-03-23 | 爆率结果区滚动条弱化与200条渐进加载 | 完成 | 已将四列列表滚动条改为更细、更弱化的官网风格，并统一为顶部显示总量/加载进度；列表与走法列首批仅加载200条，滚动到底后自动续载下一批，降低首批等待时间并提升滚动流畅度 |
| 2026-03-23 | 修复脚本注入复制模式误创建“(根目录)”文件夹 | 完成 | 已修复脚本注入复制模式对根目录占位文案的处理：当目标目录为空并保存为`(根目录)`时，执行注入会正确落到所选根目录本身，不再把`(根目录)`当作真实子目录创建 |
| 2026-03-23 | 修复爆率查询远端同步 413 限制不一致 | 完成 | 已定位网站管理“新建并同步远端”在爆率查询模块失败的根因：远端接收端仍写死 35MB 限制，而工具箱全局常量已提升为 120MB，导致爆率数据包超过 35MB 时直接返回 HTTP 413。现已统一改为使用 `REMOTE_SYNC_MAX_BODY`，并补充明确错误提示，基础版本与爆率查询同步上限保持一致 |
| 2026-03-23 | 清理源码字符串引号损坏并恢复可编译状态 | 完成 | 已继续修复 `工具箱_qt.py` 中多处中文引号损坏导致的语法错误，包括“已使用”“仅改数量/仅改时间”“版本根目录”“点击加入”“自动识别并连接”“脚本实例”等提示文案；当前已通过 `python -m py_compile` 编译校验 |
| 2026-03-23 | 增加远端同步请求体本地预检提示 | 完成 | 已在爆率查询与版本同步两条发送链路中增加请求体大小本地预检：当 JSON 请求体超过 `REMOTE_SYNC_MAX_BODY` 时，发送前直接提示当前包体约大小与 120MB 上限，并分别指向爆率数据/图片/攻略附件或版本图片等可能原因，避免无效请求后才收到远端 413 |
| 2026-03-23 | 清理爆率模块残留乱码文案 | 完成 | 已继续清理 `工具箱_qt.py` 中爆率模块残留的乱码内容：修正 `_column_mapping()` 里的“序号/名称/分类/外观”列名映射，并补齐导出/生成流程附近的关键注释文案；当前再次通过 `python -m py_compile` 与文件级 lint 校验 |
| 2026-03-23 | 补充历史乱码配置键兼容说明 | 完成 | 已继续排查 `工具箱_qt.py` 剩余乱码来源：修正爆率查询流程中残留的乱码注释，并为配置读取/迁移处保留的历史乱码键补充中文说明，明确这些字符串仅用于兼容旧版错误编码配置，避免后续误删；本次修改后再次执行 `python -m py_compile` 校验 |
| 2026-03-23 | 清理爆率导出链路最后一处乱码注释 | 完成 | 已继续清理 `工具箱_qt.py` 爆率导出链路中的残留乱码注释，将 `MonItems` 目录查找处的乱码说明恢复为可读中文；修改后再次执行 `python -m py_compile` 与文件级 lint 校验 |
| 2026-03-23 | 批量清理历史备份中的爆率乱码注释 | 完成 | 已按同一规则继续清理 `source_backups/` 与 `legacy_recovery_archive/20260319_cleanup/` 中历史备份脚本的爆率模块残留乱码，批量修正“查找 MonItems 目录”“提示用户”“获取所有查询项”“未找到 MonItems 目录”“MonItems目录”等明显可恢复文案，避免后续回溯历史版本时继续读到乱码 |
| 2026-03-23 | 继续清理历史备份中脚本注入页乱码 | 完成 | 已继续清理 `legacy_recovery_archive/20260319_cleanup/工具箱_qt.corrupted.py` 及部分 `source_backups/` 中脚本注入模板页的残留乱码，修正“根目录”“搜索模板”“新建/重命名/删除模板”“导入/导出 TXT”“保存模板”“一键注入/删除”等按钮与模板示例文案，并复查该历史文件未引入新的 lint 问题 |
| 2026-03-23 | 继续清理历史备份中微端配置页乱码 | 完成 | 已继续清理 `legacy_recovery_archive/20260319_cleanup/工具箱_qt.corrupted.py` 中微端配置页残留乱码，修正“微端配置”“选择配置模板/载入模板”“版本名称/传奇目录/补丁目录/微端模板”“自动端口/检测占用/显示/隐藏/创建/启动/关闭/删除”等界面文案，并同步修正“GOM引擎”“无缓存”“选择微端模板/保存模板/模板已保存”等相关提示；本轮复查该历史文件 lint 仍为 0 |

## 4. 运行说明
1. 确保安装 Python 3.x。
2. 安装依赖：`pip install Pillow`。
3. 运行入口文件：`python NPC_Python/main.py`。
4. 单元测试：`python NPC_Python/test_headless.py`。

## 5. 待优化项
- [x] 代码语法高亮（已实现）。
- [x] 物品/图片选择对话框（已实现）。
- [x] 图片资源加载（已实现本地 assets 目录 Mock 模式）。

## 6. QT工具箱打包（瘦身版）

### 6.1 当前采用方式（spec + 自定义 hook）
在项目根目录执行：

```powershell
.\.venv38\Scripts\python.exe -m PyInstaller --noconfirm --clean "虾米工具箱.spec"
```

关键点：
- `虾米工具箱.spec` 已启用 `hookspath=[_ROOT]`，会加载项目根目录自定义 hook。
- 瘦身核心在 `hook-PySide2.py`：仅收集最小运行集（QtCore/QtGui/QtWidgets + 必需平台与图片插件），避免把 WebEngine/3D/Multimedia 等大模块打进包。
- 目标输出仍为 onedir（`dist\虾米工具箱`），可直接被热更新流程识别。

### 6.2 生成正式 hotupdate 包（固定输出到 dist）

```powershell
$root = "D:\AI程序编辑\RJDM\dist"
$appDir = Join-Path $root "虾米工具箱"
$zip = Join-Path $root "虾米工具箱-1.2.2-hotupdate.zip"
if (Test-Path -LiteralPath $zip) { Remove-Item -LiteralPath $zip -Force }
Compress-Archive -Path $appDir -DestinationPath $zip -CompressionLevel Optimal
(Get-FileHash -Algorithm SHA256 -LiteralPath $zip).Hash
```

### 6.3 验证标准（防止体积回弹）
- 打包日志中 `Performing binary vs. data reclassification` 条目数量应明显低于全量收集模式。
- `build\虾米工具箱\Analysis-00.toc` 不应再出现 `Qt5WebEngineCore.dll`、`QtWebEngine.pyd`、`QtWebEngineWidgets.pyd`。
- 体积异常增大时优先检查 `hook-PySide2.py` 是否被改回 `collect_dynamic_libs/collect_data_files` 全量收集。
- 生成 zip 后必须重新计算并发布对应 SHA256。
